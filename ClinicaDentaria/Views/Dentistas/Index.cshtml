@model IEnumerable<ClinicaDentaria.Models.Dentista>

@{
    ViewData["Title"] = "Dentistas";
}

<h2>Dentistas</h2>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-default">Editar</a> |
                <!--<a asp-action="Details" asp-route-id="@item.Id">Detalhes</a>-->
                <a asp-controller="Agenda" asp-route-id="@item.Id" asp-action="Index" class="btn btn-default" target="_blank">Agenda</a> |
                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Excluir</a>
            </td>
        </tr>
}
    </tbody>
</table>
